# NoteVault 디자인 시스템

작성일: 2026-08-14

읽기 전용 Obsidian 볼트 뷰어(Android)의 색·타입·간격 정의. 앱 UI와 WebView 안의
노트 본문이 **같은 토큰에서 나온다** — NativeWind 색상 토큰이 CSS 변수이므로
`global.css`에 정의한 값을 렌더러 스타일시트에서 그대로 재사용한다.

콜아웃 색은 **Obsidian 코어에서 실측 추출한 값**을 쓴다(`obsidian.asar`). 볼트에서
1693개가 쓰이는 문법이므로 Obsidian에서 보던 것과 색이 달라지면 이질감이 생긴다.

## 1. 성격

깊은 차콜 캔버스 위의 조용한 읽기 표면이다. 개인용 도구이고 편집 기능이 없으므로
화면은 본문에서 비켜서는 것이 목적이다. 표면은 작은 차콜 단계로 올라가고
(`#1E1E1E` → `#262626` → `#2D2D2D`) 1pt 디바이더가 영역을 나눈다. 그림자는 진짜로
떠 있는 것(바텀시트, 메뉴)에만 쓴다.

강조색은 보라 하나다. `#7C3AED`가 액션, `#A78BFA`가 링크·활성 상태. 회색 텍스트의
바다에서 보라는 항상 "여기 누를 것이 있다" 또는 "여기 연결이 있다"를 뜻한다.

**예외: 콜아웃.** 콜아웃은 타입별 색 구분이 의미의 일부다 — warning이 주황이 아니면
warning으로 읽히지 않는다. 유일하게 보라 단일 강조 원칙을 벗어나는 지점이며,
Obsidian 코어 팔레트를 그대로 쓴다.

### 이 앱에 없는 것

원본 영감이 된 Obsidian iOS 앱과 달리 다음이 없다. 설계 문서 1장 비목표 참조.

그래프뷰 · 백링크 패널 · 커맨드 팔레트 · 소스 에디터 · 읽기/소스 토글 · 탭 스트립 ·
미저장 표시 · 노트 이름 변경 · frontmatter 편집 · iPad 멀티팬

읽기 전용이므로 편집 상태를 나타내는 색과 컴포넌트가 전부 불필요하다. frontmatter는
렌더 전에 제거하므로 표시 대상이 아니다.

## 2. 색상 토큰

라이트/다크 쌍으로 정의한다. `userInterfaceStyle: automatic`이므로 시스템 테마를
따르고, 설정에서 수동 지정도 가능하다. 출퇴근길에는 낮 시간대와 밝은 실내가 실제로
포함되므로 라이트 모드를 다크와 동등한 수준으로 정의한다.

### 표면과 텍스트

| 토큰 | 다크 | 라이트 | 용도 |
|---|---|---|---|
| `background` | `#1E1E1E` | `#FFFFFF` | 캔버스. 뷰어 본문 배경 |
| `card` | `#262626` | `#F7F7F7` | 사이드바, 설정 행, 바텀시트 본체 |
| `popover` | `#2D2D2D` | `#FFFFFF` | 모달, 메뉴, 코드블록 배경 |
| `muted` | `#363636` | `#EDEDED` | 눌린 상태, 입력 필드 채움 |
| `border` | `#363636` | `#E2E2E2` | 1pt 디바이더, 테두리 |
| `foreground` | `#DCDDDE` | `#1E1E1E` | 본문, 노트 제목, 주요 UI 텍스트 |
| `muted-foreground` | `#999999` | `#6B6B6B` | 메타데이터, 폴더 라벨, 시각 |
| `subtle-foreground` | `#858585` | `#767676` | **비활성·placeholder 전용** |

다크의 본문색은 순백이 아닌 `#DCDDDE`다. 장시간 독서에서 순백은 눈이 피로하다.

**라이트 `popover`는 `card`보다 밝지 않다.** 라이트 모드에서 캔버스가 이미 흰색이므로
표면을 더 밝게 올릴 수 없다. 대신 바텀시트·모달은 `#FFFFFF` 배경 + 1pt `border` +
그림자로 분리한다(6장 깊이 참조). 배경색만으로는 구분되지 않는다.

**`subtle-foreground`는 비활성과 placeholder에만 쓴다.** 메타데이터·버튼 텍스트에는
`muted-foreground`를 쓴다. WCAG는 비활성 요소의 대비 요구를 면제하지만, 값을 올려
(다크 3.13:1 → 4.52:1, 라이트 2.78:1 → 4.54:1) 실수로 본문에 쓰였을 때도 읽히게 했다.

### 대비 검증

주요 조합의 WCAG 명도 대비다. 본문 텍스트는 AA(4.5:1) 이상을 지킨다.

| 조합 | 다크 | 라이트 |
|---|---|---|
| `foreground` / `background` | 12.26:1 | 16.67:1 |
| `muted-foreground` / `background` | 5.85:1 | 5.33:1 |
| `subtle-foreground` / `background` | 4.52:1 | 4.54:1 |

### 강조

| 토큰 | 다크 | 라이트 | 용도 |
|---|---|---|---|
| `primary` | `#7C3AED` | `#7C3AED` | 액션 버튼, 활성 파일 좌측 바 |
| `primary-foreground` | `#FFFFFF` | `#FFFFFF` | primary 위의 텍스트 |
| `accent` | `#A78BFA` | `#6D28D9` | 위키링크, 활성 상태, 캐럿 |
| `primary-pressed` | `#6D28D9` | `#5B21B6` | 눌린 상태 |
| `primary-tint` | `rgba(124,58,237,0.14)` | `rgba(124,58,237,0.10)` | 선택 행 배경, 태그 칩 |

보라는 양쪽 테마에서 동일하게 읽히므로 `primary`는 값을 공유한다. `accent`는 라이트에서
밝은 배경 대비를 위해 어두운 쪽으로 내린다.

### 본문 의미색

| 토큰 | 다크 | 라이트 | 용도 |
|---|---|---|---|
| `link-internal` | `#A78BFA` | `#6D28D9` | `[[위키링크]]` |
| `link-external` | `#7C9CBF` | `#3B6EA5` | `https://` 외부 링크 |
| `code-fg` | `#DCDDDE` | `#1E1E1E` | 인라인·블록 코드 텍스트 |
| `code-bg` | `#2D2D2D` | `#F3F3F3` | 코드블록 배경 |
| `highlight` | `rgba(255,213,79,0.25)` | `rgba(255,213,79,0.45)` | `==하이라이트==` (33개 노트) |
| `quote-bar` | `#7C3AED` | `#7C3AED` | `>` 인용문 좌측 3pt 바 |
| `table-border` | `#363636` | `#E2E2E2` | 표 테두리 (1171개 노트가 표 사용) |

**위키링크는 미해결 상태를 색으로 구분하지 않는다.** 설계 문서 8장에서 렌더 시점에
링크 대상 존재 여부를 조회하지 않기로 했다(긴 노트에서 불필요한 비용). 탭한 뒤
로컬에 없으면 시트로 알린다. 따라서 렌더 시점에 dim 처리할 근거가 없다.

### 상태

| 토큰 | 다크 | 라이트 | 용도 |
|---|---|---|---|
| `success` | `#4ADE80` | `#15803D` | 동기화 완료 |
| `error` | `#F87171` | `#DC2626` | 동기화 실패, 401 배너 |
| `warning-state` | `#FBBF24` | `#A16207` | 저장공간 경고, 부분 실패 |

라이트 모드 값은 흰 배경에서 AA를 통과하도록 어둡게 잡았다(`success` 5.02:1,
`warning-state` 4.92:1, `error` 4.83:1). 밝은 쪽 값(`#16A34A`, `#D97706`)은 각각
3.30:1 · 3.19:1로 본문 크기 텍스트에 부족하다.

## 3. 콜아웃 색 (Obsidian 코어 실측)

볼트에서 519개 노트에 1693개가 쓰인다. 아래 값은 `obsidian.asar`에서 추출한
`--color-*` 정의이며 Obsidian에서 보는 것과 동일하다.

### 기준 팔레트 (좌측 바 · 아이콘 · 배경 tint)

| 색 | 다크 | 라이트 |
|---|---|---|
| blue | `#027aff` | `#086ddd` |
| cyan | `#53dfdd` | `#00bfbc` |
| green | `#44cf6e` | `#08b94e` |
| orange | `#e9973f` | `#ec7500` |
| red | `#fb464c` | `#e93147` |
| purple | `#a882ff` | `#7852ee` |
| gray | `#9e9e9e` | `#9e9e9e` |

### 제목 텍스트 색 — 역할을 분리한다

**Obsidian 원색을 라이트 모드의 제목 텍스트로 그대로 쓰면 대비가 부족하다.** 흰 배경
기준 실측값이다.

| 색 | 원색 대비 | 판정 | 제목 전용 색 | 보정 후 |
|---|---:|---|---|---:|
| cyan | 2.29:1 | 실패 | `#007A78` | 5.18:1 |
| green | 2.61:1 | 실패 | `#087F3A` | 5.11:1 |
| gray | 2.68:1 | 실패 | `#5F6368` | 6.05:1 |
| orange | 2.95:1 | 실패 | `#A64B00` | 5.79:1 |
| red | 4.20:1 | 실패 | `#C6283D` | 5.57:1 |
| purple | 4.95:1 | 통과 | `#5B21B6` | 8.98:1 |
| blue | 4.95:1 | 통과 | `#086ddd` 유지 | 4.95:1 |

따라서 라이트 모드에서만 요소별로 색을 나눈다.

```
좌측 바 · 아이콘 · 배경 tint   원색 유지        ← Obsidian 느낌을 유지
제목 텍스트                   위 전용 색       ← 가독성 확보
본문 텍스트                   foreground
```

**다크 모드는 보정하지 않는다.** `#1E1E1E` 배경에서 원색이 전부 AA를 통과한다
(cyan 10.29 · green 8.25 · orange 7.12 · gray 6.22 · purple 5.80 · red 4.83 ·
blue 4.15). blue만 4.15로 본문 크기 기준에는 살짝 못 미치나 콜아웃 제목은 16pt
weight 600이므로 large text 기준(3:1)을 충족한다.

### 타입 매핑

Obsidian 코어의 `data-callout` → 색 매핑이다. 볼트 실사용 개수를 함께 적었다.

| 타입 | 색 | Lucide 아이콘 | 볼트 사용 |
|---|---|---|---|
| `note` (default) | blue | `pencil` | 360 |
| `info` | blue | `info` | 184 |
| `todo` | blue | `check-circle-2` | 0 |
| `abstract` `summary` `tldr` | cyan | `clipboard-list` | 90 |
| `tip` `hint` | cyan | `flame` | 248 |
| `important` | cyan | `flame` | 244 |
| `success` `check` `done` | green | `check` | 17 |
| `question` `help` `faq` | orange | `help-circle` | 150 |
| `warning` `caution` `attention` | orange | `alert-triangle` | 319 |
| `failure` `fail` `missing` | red | `x` | 0 |
| `danger` `error` | red | `zap` | 7 |
| `bug` | red | `bug` | 0 |
| `example` | purple | `list` | 37 |
| `quote` `cite` | gray | `quote` | 1 |

**볼트에만 있는 비표준 타입.** `warn`(35)과 `primary`(1)는 Obsidian 코어 매핑에
없으므로 `default`(blue)로 떨어진다. `warn`은 의미상 주황이 맞으므로 렌더러에서
`warning`과 같은 그룹으로 별칭 처리한다. `primary`는 1개뿐이라 기본값으로 둔다.

주의: `question`은 **주황**이고 `tip`·`important`는 **cyan**이다. 직관적으로 보라·초록을
떠올리기 쉬우나 Obsidian 코어 값이 위와 같다.

### 콜아웃 구조 (Obsidian 실측)

```
배경        rgba(색, 0.1)
좌측 바     3pt solid rgb(색)      ← Obsidian은 border-width 0 + 배경만 쓰지만
                                     모바일 가독성을 위해 좌측 바를 추가한다
제목 텍스트  rgb(색), weight 600
본문 텍스트  foreground
아이콘      rgb(색), 16pt
패딩        12pt (좌측 바 쪽 16pt)
radius      4pt
```

접기(`[!tip]-`)는 `<details>`로 렌더되며 제목 우측에 `chevron-down`을 두고 열림 상태에서
180° 회전한다.

## 4. 타이포그래피

### 폰트

**시스템 폰트만 쓴다. 웹폰트를 번들하지 않는다.**

```
본문·UI     system-ui, Roboto, sans-serif
모노스페이스  'SF Mono', 'Roboto Mono', Menlo, Consolas, monospace
```

원본 영감은 Inter + JetBrains Mono 두 체계를 정체성으로 삼지만, 그 근거는 소스
에디터와 커맨드 팔레트였다. 이 앱은 읽기 전용이라 모노스페이스가 필요한 곳이
**코드블록 하나**다. 웹폰트 두 벌을 APK에 넣을 이유가 없다.

또한 볼트 본문이 대부분 한글이다. Inter에는 한글 글리프가 없어 한글은 시스템 폰트로
떨어지므로, 라틴 문자만 Inter가 되어 오히려 섞인 느낌이 난다.

### 계층

| 역할 | 크기 | 굵기 | 행간 | 자간 |
|---|---|---|---|---|
| 노트 제목 (H1) | 26 | 700 | 1.25 | -0.3 |
| 본문 H1 | 24 | 700 | 1.3 | -0.2 |
| 본문 H2 | 20 | 700 | 1.3 | -0.2 |
| 본문 H3 | 17 | 600 | 1.3 | -0.1 |
| 본문 H4~H6 | 16 | 600 | 1.4 | 0 |
| **본문** | **16** | **400** | **1.7** | **0** |
| 코드블록 | 14 | 400 | 1.55 | 0 |
| 인라인 코드 | 14 | 400 | — | 0 |
| 표 셀 | 14 | 400 | 1.45 | 0 |
| 콜아웃 제목 | 16 | 600 | 1.4 | 0 |
| 파일트리 행 | 15 | 400 | 1.3 | -0.1 |
| 섹션 헤더 (대문자) | 11 | 700 | 1.2 | 0.6 |
| 메타데이터 | 13 | 400 | 1.3 | 0 |
| 버튼 | 15 | 600 | 1.0 | -0.1 |

**본문 행간을 1.7로 올린다.** 원본은 1.6이지만 한글은 라틴 문자보다 세로로 빽빽해
더 필요하다. 설정의 글자 크기 항목은 본문 16pt를 기준으로 ±2단계를 제공한다.

### 원칙

- 제목 계층은 **크기와 굵기**로만 표현한다. 색은 `foreground` 고정
- 굵기는 400 / 600 / 700만 쓴다. light·black 없음
- 모노스페이스는 코드에만. UI에 쓰지 않는다

## 5. 간격과 형태

### 간격

기본 단위 4pt. 스케일 `4 · 6 · 8 · 12 · 16 · 20 · 24 · 32 · 40`

- 본문 좌우 여백 16pt
- 사이드바 패널 내부 12pt
- 파일트리 깊이당 들여쓰기 16pt
- 문단 간격 16pt, 제목 위 24pt / 아래 8pt

### radius

| 값 | 용도 |
|---|---|
| 0 | 디바이더, 전체폭 본문 |
| 4 | 콜아웃, 코드블록, 인라인 코드 |
| 6 | 버튼, 입력 필드, 체크박스 |
| 10 | 바텀시트, 모달 |
| 999 | 태그 칩, 필터 칩 |

### 깊이

| 단계 | 처리 | 용도 |
|---|---|---|
| 0 | 그림자 없음 | 본문, 파일트리 행 |
| 1 | `background` → `card` + 1pt `border` | 사이드바, 설정 섹션 |
| 2 | `rgba(0,0,0,0.4) 0 8px 24px` + 1pt | 메뉴, 팝오버 |
| 3 | `rgba(0,0,0,0.6) 0 20px 56px` + 1pt | 바텀시트, 모달 |
| 스크림 | `rgba(0,0,0,0.5)` | 사이드바·모달 뒤 |

**깊이는 표면 단계로 표현하고 그림자는 진짜 떠 있는 것에만 쓴다.** 라이트 모드에서는
차콜 단계 대신 `#FFFFFF` → `#F7F7F7`로 내려가고 `border`가 경계를 담당한다.

표면 단계의 대비는 의도적으로 미세하다(다크 1.10:1, 라이트 1.07:1). 조용한 성격이
목적이므로 값을 키우지 않는다. 대신 배경만으로 구분이 안 되는 지점을 다음으로 보완한다.

- 카드·설정 그룹은 배경 단계 + **1pt `border`를 반드시 함께** 쓴다. 라이트 모드에서
  배경 차이가 1.07:1이라 border가 실질적인 경계다
- 바텀시트·모달은 그림자를 유지한다. 라이트 모드에서 `popover`가 `background`와 같은
  `#FFFFFF`이므로 그림자와 border 없이는 배경에 묻힌다
- 활성 파일 행의 보라 좌측 2pt 바를 유지한다. 배경 tint만으로는 약하다

## 6. 컴포넌트

이 앱에 실제로 필요한 것만 정의한다. 설계 문서 7장 화면 구성에 대응한다.

### 파일트리 행 (사이드바 · 주 내비게이션)

```
높이 40pt (터치 타깃 확보)
레이아웃  들여쓰기(깊이×16) → chevron(폴더) → 아이콘 14pt → 이름 15pt → 여백
기본      배경 투명
활성      배경 primary-tint + 좌측 2pt primary 바
누름      배경 muted
폴더 토글  chevron 90° 회전 + 자식 높이 애니메이션 180ms
```

원본은 32pt지만 Android 터치 타깃 최소 48dp를 고려해 40pt + hit slop으로 올린다.

### 뷰어 본문 — 마크다운 (WebView)

아래는 `.md`에만 적용된다. `.html`은 8장의 격리 정책에 따라 원본 CSS를 유지한다.

```
배경      background, 전체폭, 좌우 16pt
본문      16pt / 1.7
코드블록   code-bg, radius 4, 12pt 패딩, 가로 스크롤
인용문     좌측 3pt quote-bar + muted-foreground 텍스트
표        가로 스크롤 컨테이너, 1pt table-border
이미지     max-width 100%, 실패 시 "이미지 없음" 자리표시
콜아웃     3장 참조
```

표(1171개 노트)와 코드블록은 폰 폭을 넘기므로 **각각 독립 가로 스크롤 컨테이너**에
넣는다. 본문 자체가 가로로 스크롤되면 안 된다.

### 구독 관리 트리 (설정 하위)

```
행 높이   52pt (2줄: 폴더명 + 용량 정보)
1줄       chevron → 폴더명 15pt → 체크박스 (우측)
2줄       "노트 42 · 이미지 8 · 4.1MB" 13pt muted-foreground
체크박스   6pt radius, 체크 시 primary 채움
부분 선택  primary 테두리 + 중앙 가로 바 (자식 일부만 구독)
```

FlashList로 가상화한다(원격 트리 3555개). 용량 표시는 설계 문서 6장 참조.

### 동기화 진행 표시

```
위치      사이드바 하단 고정 또는 상단 배너
진행 중    회전 스피너 + "12 / 47" + 파일명 (1줄 말줄임) + 취소
완료      "새 노트 5 · 갱신 3 · 삭제 1", success 색, 3초 후 자동 소멸
오프라인   "오프라인 상태입니다", muted-foreground
실패      "3개 실패 — 다시 시도" 버튼, error 색
401(자동)  배너만. 읽기를 막지 않음 (설계 문서 9장)
```

### 설정 행

```
높이      52pt
레이아웃   아이콘 18pt → 라벨 15pt → 값/스위치/chevron (우측)
그룹      card 배경 + radius 10, 그룹 사이 24pt
섹션 헤더  11pt 700 대문자 muted-foreground, 자간 0.6
```

### Device Flow 인증 화면

```
코드 표시  모노스페이스 32pt 700, 자간 4pt, 중앙 정렬
          card 배경 + radius 10 + 24pt 패딩
복사 버튼  코드 아래, ghost 스타일 + Copy 아이콘
안내       "GitHub에서 이 코드를 입력하세요" 15pt muted-foreground
주 액션    "GitHub 열기" primary 버튼 전체폭
상태       승인 대기 중 스피너 + "승인을 기다리는 중…"
```

8자리 코드는 사용자가 다른 기기에서 옮겨 적는 유일한 지점이므로 자간을 넓혀 읽기
쉽게 한다.

### 버튼

```
Primary   primary 배경, primary-foreground 텍스트, 15pt 600
          높이 44pt, radius 6, 좌우 16pt
          누름: primary-pressed + scale 0.98
          비활성: muted 배경, subtle-foreground 텍스트
Ghost     투명 배경, 아이콘 18pt muted-foreground
          누름: muted 배경, accent 아이콘
          터치 타깃 44pt
```

원본의 34pt는 iOS 기준이다. Android 최소 48dp 권장에 맞춰 44pt + hit slop으로 둔다.

## 7. 모션

| 대상 | 값 |
|---|---|
| 사이드바 슬라이드 | translateX 스프링 220ms (response 0.35, damping 0.9) + 스크림 페이드 |
| 폴더 펼침 | chevron 0°→90° + 자식 높이 180ms ease |
| 콜아웃 접기 | 높이 180ms ease |
| 바텀시트 | 아래에서 위로 250ms ease-out |
| 동기화 스피너 | 연속 회전 1000ms linear |
| 완료 배너 | 페이드인 150ms → 3초 유지 → 페이드아웃 300ms |
| 테마 전환 | 즉시 (크로스페이드 없음 — WebView와 RN이 동기화되지 않으면 어긋나 보인다) |

**당겨서 새로고침**은 플랫폼 기본 동작을 쓴다. 커스텀하지 않는다.

## 8. WebView 토큰 전달과 CSS 격리

앱 UI와 본문 테마가 한 출처에서 나오도록 다음 경로를 쓴다.

```
global.css (--nv- 토큰 + @theme)       ← 단일 출처
        │
        ├── NativeWind → RN 컴포넌트 (사이드바, 설정, 버튼)
        │
        └── 렌더러 빌드 시 CSS 변수로 추출
                → assets/renderer/dist/theme.css
                → WebView (마크다운 본문, 콜아웃, 코드블록)
```

### 마크다운과 `.html`을 같은 CSS 범위에 두지 않는다

`.md`는 우리가 HTML을 생성하므로 테마를 강제해도 안전하다. 그러나 `.html`은 **작성자가
만든 완결된 문서**이고 자체 CSS를 갖는다. 여기에 우리 테마를 얹으면 문서의 색·표·
코드블록·버튼 스타일이 깨진다.

| 콘텐츠 | 테마 처리 |
|---|---|
| `.md` (렌더러가 생성) | NoteVault 테마 강제 적용 |
| `.html` (볼트 파일) | **원본 CSS 유지. 우리 테마를 주입하지 않는다** |

`.html`은 별도 WebView 인스턴스로 열고 `theme.css`를 로드하지 않는다.

**`.html`의 배경은 흰색으로 고정한다.** 원본 CSS가 배경색을 지정하지 않는 경우가 흔하다
(대부분 `color`만 정하고 배경은 브라우저 기본에 맡긴다). 이때 우리가 다크 배경을 깔면
**어두운 배경에 어두운 글씨**가 되어 본문이 보이지 않는다. HTML 문서는 스스로 완결된
문서이므로 앱 테마를 섞지 않고 브라우저 기본 환경(흰 배경)을 제공하는 것이 안전하다.
다크 모드에서 흰 문서가 열리는 것은 의도된 동작이다.

### 변수 이름을 `--nv-`로 접두한다

`--background` 같은 일반적인 이름은 `.html` 문서가 같은 이름을 쓸 때 충돌한다.

```css
html[data-nv-theme="dark"]  { --nv-background: #1E1E1E; --nv-foreground: #DCDDDE; }
html[data-nv-theme="light"] { --nv-background: #FFFFFF; --nv-foreground: #1E1E1E; }
```

적용 범위도 클래스로 한정한다. 전역 셀렉터를 쓰지 않는다.

```css
/* 좋음 — 우리가 생성한 영역에만 */
.nv-markdown { background: var(--nv-background); color: var(--nv-foreground); }

/* 금지 — 문서 작성자의 스타일을 덮어쓴다 */
body, p, div, span, a { color: var(--nv-foreground); }
*             { color: var(--nv-foreground); }
```

### WebView 자동 다크닝을 끈다

Android WebView의 Force Dark(`algorithmicDarkening`)가 켜져 있으면 우리가 CSS로 다크를
적용한 뒤 WebView가 색을 **한 번 더** 변환한다. 결과는 배경이 과도하게 어두워지고,
이미지·SVG 색이 반전되고, 코드 하이라이팅이 깨진다. 테마를 직접 관리하므로 끈다.

`.html`용 WebView도 함께 끈다. 문서의 원본 색을 시스템이 임의로 뒤집으면 작성자가
의도한 대비가 무너진다.

### 앱(RN) 쪽 토큰 파이프라인의 함정 — 실측으로 확인된 것

`global.css`의 `light-dark()` 토큰이 앱 UI 에 도달하기까지 세 곳에서 다크 값이
소실될 수 있다. 전부 실기기에서 실제로 발생했고 재발 방지 장치가 있다.

| 함정 | 증상 | 방지 장치 |
|---|---|---|
| metro `inlineVariables: false` | 변수의 다크 값이 컴파일에서 소실 | 옵션 사용 금지 + 카나리 테스트 (`theme.test.tsx`) |
| `browserslist` 미설정 | lightningcss 가 light-dark 를 폴리필로 바꿔 라이트 폴백만 남음 | `package.json` `"browserslist": ["chrome 138"]` |
| react-native-css 가 Appearance 이벤트 미수신 | 헤더만 바뀌고 className 배경 고정 | `Appearance.setColorScheme` + css `colorScheme.set` **이중 호출** (`store/settings.ts` applyTheme) |

### 테마 전환은 `data-nv-theme`으로만 한다

RN이 현재 테마를 `postMessage`로 알리고 WebView가 속성을 바꾼다.

```js
document.documentElement.dataset.nvTheme = 'dark'
```

**`prefers-color-scheme`에 의존하지 않는다.** 설정에서 다크/라이트를 수동 지정할 수
있으므로 시스템 테마와 앱 테마가 다를 수 있다. `prefers-color-scheme`만 쓰면 앱은
라이트인데 본문만 다크로 렌더되는 상황이 생긴다. 시스템 추종은 RN 쪽에서 판단해
`data-nv-theme` 값으로 내려보낸다.

## 9. 하지 말 것

- 두 번째 브랜드 강조색을 만들지 않는다 — 보라 하나. 콜아웃만 예외
- 제목을 색으로 구분하지 않는다 — 크기와 굵기로
- 패널·행에 그림자를 넣지 않는다 — 표면 단계 + 1pt 디바이더
- light·black 굵기를 쓰지 않는다 — 400 / 600 / 700
- 웹폰트를 번들하지 않는다 — 시스템 폰트로 충분하다
- 본문이 가로로 스크롤되게 두지 않는다 — 표·코드블록만 각자 스크롤
- 편집 상태를 나타내는 색·컴포넌트를 만들지 않는다 — 읽기 전용이다
- 콜아웃 색을 임의로 바꾸지 않는다 — Obsidian에서 보던 것과 같아야 한다
- 하단 탭바를 넣지 않는다 — 사이드바 드로어가 내비게이션이다
- **`.html` 문서에 우리 테마를 주입하지 않는다** — 원본 CSS를 존중하고 배경은 흰색
- **전역 셀렉터(`*`, `body`, `p`, `a`)로 색을 강제하지 않는다** — `.nv-` 클래스 범위 안에서만
- **`prefers-color-scheme`만으로 테마를 판단하지 않는다** — `data-nv-theme`으로 통제
- **WebView 자동 다크닝을 켜두지 않는다** — 이중 변환으로 색이 깨진다
- `subtle-foreground`를 메타데이터·버튼에 쓰지 않는다 — `muted-foreground`를 쓴다
